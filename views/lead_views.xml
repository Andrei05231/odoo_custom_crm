<odoo>
    <record id="crm_lead_form_add_contact_ids" model="ir.ui.view">
        <field name="name">crm.lead.form.add-contact-ids</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//group[@name='lead_partner']" position="replace">
                <group name="lead_partner">
                    <field name="contact_ids" widget="many2many_tags" domain="[('is_company','=',True)]"/>
                    <field name="designer_ids" widget="many2many_tags" domain='[("is_company", "=", False)]'/>
                    <field name="sub_contractant_ids" widget="many2many_tags" />
                    <field name="partner_id" invisible="1"/>
                </group>
            </xpath>


            <xpath expr="//group[@name='opportunity_partner']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//field[@name='email_from']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//field[@name='phone']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>


        </field>
    </record>
</odoo>
